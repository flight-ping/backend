services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flightping-app
    depends_on:
      db:
        condition: service_healthy
      crawler:
        condition: service_started
    environment:
      SERVER_PORT: 8080
      SPRING_PROFILES_ACTIVE: docker
      CRAWLER_URL: http://crawler:8000
    ports:
      - "8080:8080"

  crawler:
    build:
      context: ../crawler
      dockerfile: Dockerfile
    container_name: flightping-crawler
    ports:
      - "8000:8000"

  db:
    image: postgres:16-alpine
    container_name: flightping-db
    environment:
      POSTGRES_DB: flightping
      POSTGRES_USER: flightping
      POSTGRES_PASSWORD: flightping
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flightping -d flightping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
